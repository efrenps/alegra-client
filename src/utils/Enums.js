export const SUBSCRIPTION_ACTION_TYPES = Object.freeze({
    CREATED: 'CREATED',
    UPDATED: 'UPDATED',
    DELETED: 'DELETED',
});

export const MENU_ORDER_STATUS = Object.freeze({
    PENDING: 'pending',
    STORAGE: 'storage',
    COOKING: 'cooking',
    READY: 'ready',
    DELIVERED: 'delivered',
});
